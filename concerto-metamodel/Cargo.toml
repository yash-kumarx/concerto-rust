[package]
name = "concerto-metamodel"
version = "3.12.8"
edition = "2024"
authors = ["Accord Project <accordproject.org>"]
description = "Concerto Metamodel types generated from the `concerto-metamodel` package."
license = "Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
chrono = "0.4"
